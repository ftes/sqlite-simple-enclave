enclave {
    trusted {
        /* define ECALLs here. */
		public void dummy_root_ecall();
    };

    untrusted {
        /* define OCALLs here. */

		/* should a wstring be used here? */
		void log_ocall(int errCode, [in, string] char* formattedString);
    };
};
